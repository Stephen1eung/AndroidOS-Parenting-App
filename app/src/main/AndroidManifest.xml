<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.cmpt276.parentapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentApp">
        <activity
            android:name=".UI.FlipHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".UI.FlipCoinActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.TimeoutTimerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.EditChildActivity"
            android:exported="false"
            android:parentActivityName=".UI.ConfigureMyChildrenActivity" />
        <activity
            android:name=".UI.AddChildActivity"
            android:exported="false"
            android:parentActivityName=".UI.ConfigureMyChildrenActivity" />
        <activity
            android:name=".UI.ConfigureMyChildrenActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>